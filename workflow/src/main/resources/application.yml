server:
  port: 8081
spring:
  profiles:
    active: pro
  application:
    name: workflow
  activiti:
    database-schema-update: true
    history-level: full
    db-history-used: true
logging:
  level:
    com:
      licon: debug
    org:
      activiti:
        engine:
          impl:
            persistence:
              entity: debug
  config: classpath:logback-spring.xml