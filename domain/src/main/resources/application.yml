spring:
  application:
    name: domain
  datasource:
    dynamic:
      hikari:
        connection-test-query: select 1
      primary: write
      strict: false
  profiles:
    active: pro
logging:
  level:
    com:
      licon:
        domain: debug
  config: classpath:logback-spring.xml

